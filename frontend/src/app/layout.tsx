import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "FreshPredict - AI Inventory Management",
  description: "AI-driven inventory decision-support system for Malaysian SME grocers. Reduce food waste, optimize stock, and meet 2026 ESG compliance.",
  keywords: ["inventory management", "AI forecasting", "food waste reduction", "ESG compliance", "Malaysian SME"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
